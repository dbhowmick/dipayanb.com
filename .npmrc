# MeldUI is published to the GitHub Package Registry.
# Auth (//npm.pkg.github.com/:_authToken=...) is read from the user-level ~/.npmrc
# or the NPM_TOKEN env var in CI — never commit a token here.
@meldui:registry=https://npm.pkg.github.com
